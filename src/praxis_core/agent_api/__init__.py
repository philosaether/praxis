"""
Praxis Agent API — JSON-first, operation-focused API for AI agents.

Separate from the web BFF (praxis_core/api/) which serves the HTMX UI.
Both APIs share the same persistence layer and auth.
"""
